name: travixx
description: "Akıllı Turizm Platformu"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.11.5 <5.0.0'

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.3.0
  supabase_flutter: ^2.8.4
  go_router: ^14.6.2
  flutter_riverpod: ^2.6.1
  mobile_scanner: ^7.0.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.2
  http: ^1.2.2
  shared_preferences: ^2.3.4
  image_picker: ^1.1.2
  web: ^1.1.0

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true